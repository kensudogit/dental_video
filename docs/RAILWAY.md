# Railway ?????Dental Video?

## ??: ??? 1 ????

1. [Railway](https://railway.com) ? **New Project** ? **Deploy from GitHub**???? CLI ? `railway up`?
2. ???? **Settings**:
   - **Root Directory**: ????????????
   - **Config file path**: `/railway.toml`
3. **+ New** ? **Database** ? **PostgreSQL** ???
4. PostgreSQL ? `DATABASE_URL` ? Web ????? **Variable Reference** ???
5. Web ???? **Variables**????:

| ?? | ?? |
|------|------|
| `JWT_SECRET` | ???????????????? |
| `DATABASE_URL` | Postgres ??????? |

6. ??: `OPENAI_API_KEY`?AI Board??`CORS_ORIGINS` / `APP_PUBLIC_URL`??? URL?

> **????? `API_URL` ???**??????? Go API ? 8081?Next.js ? `PORT`?

7. **Settings** ? **Networking** ? **Generate Domain**
8. ??: `https://<your-domain>/status` ? `"ok": true`
9. ????????????: `demo@sakura-dental.jp` / `demo1234`

## 2 ??????????

| ???? | Root Directory | Config file |
|----------|----------------|-------------|
| api | `backend` | `/backend/railway.toml` |
| web | `frontend` | `/frontend/railway.toml` |

Web ? `API_URL=https://<api>.up.railway.app` ????**https**????????????

## CLI

```powershell
cd C:\devlop\dental_video
railway login
railway init
railway up
```

??? Dashboard ???:

```powershell
railway variables set JWT_SECRET=your-secret-here
```

## ????????

| ?? | ?? |
|------|------|
| `/` ? JSON ?? | Root ????? `/railway.toml` + `Dockerfile.unified` ??? |
| `/status` ? API ?? | ??? `[unified] API ready` ????`DATABASE_URL` ??? API ????????? |
| ???????? | `DATABASE_URL` + `JWT_SECRET` ????????? |
| GraphQL 502 | ????? `API_URL` ?**??** |

??????: `.env.railway.example`
